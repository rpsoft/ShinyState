% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hooks.R
\name{useState}
\alias{useState}
\alias{useReducer}
\alias{useEffect}
\alias{useMemo}
\alias{useCallback}
\title{Component State}
\usage{
useState(...)

useReducer(reducer, initial)

useEffect(fn, deps = NULL)

useMemo(fn, deps)

useCallback(input_id, fn)
}
\arguments{
\item{...}{Named initial values. Each name becomes a state field.}

\item{reducer}{Function \verb{(state, action)} returning the new state.}

\item{initial}{Initial reducer state.}

\item{fn}{Handler function. Receives the state accessor as the first
argument. When wired through a \verb{bind*()} helper that sends a value, the
event value is passed as the second argument.}

\item{deps}{Character vector of state field names, or \code{NULL} to run once on
mount. Dependency values are read from the component state store.}

\item{input_id}{Callback id (without namespace). Pair with \code{\link[=bindButton]{bindButton()}} or a
\verb{bind*()} input helper in the UI — do not use raw Shiny inputs directly
inside \code{render()}.}
}
\value{
Outside of an active render cycle, returns a state \emph{specification} object
passed to \code{\link[=component]{component()}}. Inside \code{render()}, returns a \link{shinystate_state}
accessor for reading and updating values.
}
\description{
Declares initial state for a \code{\link[=component]{component()}} (declarative style) or reads /
updates state during render (hook style).
}
\examples{
# Declarative specification (used in component())
useState(page = 1L, filter = NULL)

# Runtime usage inside render() is shown in ?component
}
