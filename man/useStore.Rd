% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/store.R
\name{useStore}
\alias{useStore}
\title{Use a shared store inside a component}
\usage{
useStore(store)
}
\arguments{
\item{store}{A store created with \code{\link[=createStore]{createStore()}}.}
}
\value{
A state accessor: read fields with \verb{$field}, write with \verb{$set()}
(which notifies every subscribed component).
}
\description{
Subscribes the calling component to a \code{\link[=createStore]{createStore()}} store and returns a
state accessor for it. The component re-renders when the store changes, and
the subscription is cleaned up automatically when the session ends. Call
inside a component \code{render()} function.
}
\seealso{
\code{\link[=createStore]{createStore()}}
}
