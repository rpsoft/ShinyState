% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/component.R
\name{component}
\alias{component}
\alias{componentUI}
\alias{componentServer}
\title{Define a Shiny Component}
\usage{
component(id, ..., render, computed = list())

componentUI(id)

componentServer(
  id,
  initial_state = list(),
  effect_specs = list(),
  render_fn,
  computed = list()
)
}
\arguments{
\item{id}{Component / module id.}

\item{...}{Declarations: \code{useState()} for initial state, \code{effect()} or
\code{useEffect()} for side effects, and optionally additional hook calls inside
\code{render}.}

\item{render}{Function returning UI (a \link[htmltools:tag]{htmltools::tag} or list of tags).
Receives a \link{shinystate_state} accessor when declarative \code{useState()} is
provided, otherwise call \code{useState()} inside the function. A second
argument \code{ns} provides the namespace function.}

\item{computed}{Named list of functions \verb{function(state)} exposing derived
values as \code{state$name}. Each is cached and recomputed only when state
changes.}

\item{initial_state}{Named list of initial state values.}

\item{effect_specs}{List of effect specifications from \code{\link[=useEffect]{useEffect()}}.}

\item{render_fn}{Render function.}
}
\value{
A list with \code{ui} and \code{server} functions suitable for
\code{\link[shiny:moduleServer]{shiny::moduleServer()}}, or use \code{\link[=componentUI]{componentUI()}} / \code{\link[=componentServer]{componentServer()}} helpers.
}
\description{
Creates a Shiny module that renders UI using a React-inspired hook model.
State, effects, and rendering are declared declaratively; Shiny reactivity is
handled internally so you never write \code{\link[shiny:reactive]{shiny::reactive()}} or
\code{\link[shiny:observe]{shiny::observe()}}.
}
\details{
The server function returned by \code{componentServer()} has signature
\verb{function(input, output, session, is_active = NULL)}. \code{is_active} is an
optional reactive expression returning \code{TRUE} while the component should be
active; when it returns \code{FALSE} the component is dormant (no rendering,
effects, or event handling) while its state is preserved. It is supplied
automatically by \code{\link[=serve]{serve()}} and \code{\link[=serve_dormant]{serve_dormant()}}.
}
\examples{
\dontrun{
counter <- component(
  id = "counter",
  state = useState(count = 0L),
  effect(
    deps = "count",
    function(state) {
      message("Count is now: ", state$count)
    }
  ),
  render = function(state, ns) {
    useCallback("inc", function(s) {
      s$set(count = s$count + 1L)
    })
    shiny::tagList(
      shiny::h3(paste("Count:", state$count)),
      shiny::actionButton(ns("inc"), "Increment")
    )
  }
)

shiny::shinyApp(
  ui = shiny::fluidPage(mount(counter)),
  server = function(input, output, session) serve(counter, input, output, session)
)
}
}
