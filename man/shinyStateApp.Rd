% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app.R
\name{shinyStateApp}
\alias{shinyStateApp}
\title{Build a Shiny app from components}
\usage{
shinyStateApp(..., title = NULL, routing = c("hash", "none"))
}
\arguments{
\item{...}{Components created with \code{\link[=component]{component()}}. For a multi-page app, name
them (\verb{dashboard = counter, search = filter}); names become page ids.}

\item{title}{App title, shown in the navbar / title panel.}

\item{routing}{\code{"hash"} (default) enables \verb{#!/page} URL routing for
multi-page apps; \code{"none"} disables it. Ignored for single-page apps.}
}
\value{
A \code{\link[shiny:shinyApp]{shiny::shinyApp()}} object.
}
\description{
Assembles a complete \code{\link[shiny:shinyApp]{shiny::shinyApp()}} from one or more \code{\link[=component]{component()}}s,
wiring up the UI and server for you.
}
\details{
\itemize{
\item \strong{One component} (named or not) becomes a single-page app: the component
mounted in a \code{\link[shiny:fluidPage]{shiny::fluidPage()}}, served with \code{\link[=serve]{serve()}}.
\item \strong{Several named components} become a multi-page app: one
\code{\link[shiny:tabPanel]{shiny::tabPanel()}} per component in a \code{\link[shiny:navbarPage]{shiny::navbarPage()}}, served with
\code{\link[=serve_dormant]{serve_dormant()}} so hidden pages stay dormant. Component names become tab
\code{value}s (and, prettified, tab labels). With \code{routing = "hash"} the active
tab syncs with a \verb{#!/page} URL.
}
}
\examples{
\dontrun{
shinyStateApp(
  title = "My App",
  dashboard = counter_component,
  search = filter_component
)
}
}
\seealso{
\code{\link[=component]{component()}}, \code{\link[=serve_dormant]{serve_dormant()}}, \code{\link[=router_server]{router_server()}}
}
