% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lifecycle.R
\name{serve_dormant}
\alias{serve_dormant}
\title{Serve components with dormant lifecycle}
\usage{
serve_dormant(..., session, input, output, navbar)
}
\arguments{
\item{...}{Named components created with \code{\link[=component]{component()}}. Names must match tab
\code{value} arguments.}

\item{session, input, output}{Shiny server function arguments.}

\item{navbar}{Character id of the \code{navbarPage()} / \code{tabsetPanel()}.}
}
\description{
Registers component servers that stay \strong{dormant} while their tab or page is
hidden. Dormant components do not render, run effects, or handle events.
Component state is preserved when a tab becomes active again.
}
\details{
Names of components must match the \code{value} of each \code{\link[shiny:tabPanel]{shiny::tabPanel()}} in the
corresponding \code{\link[shiny:navbarPage]{shiny::navbarPage()}} or \code{\link[shiny:tabsetPanel]{shiny::tabsetPanel()}}.
}
\examples{
\dontrun{
shinyApp(
  ui = navbarPage(
    id = "pages",
    tabPanel("Dashboard", mount(counter), value = "dashboard"),
    tabPanel("Search", mount(filter), value = "search")
  ),
  server = function(input, output, session) {
    serve_dormant(
      session = session, input = input, output = output,
      navbar = "pages",
      dashboard = counter,
      search = filter
    )
  }
)
}
}
