% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/router.R
\name{router_server}
\alias{router_server}
\title{Sync a navbar or tabset with the browser URL hash}
\usage{
router_server(session, input, navbar, tabs = NULL, default = NULL, hash = NULL)
}
\arguments{
\item{session, input}{Shiny server function arguments.}

\item{navbar}{Character id of the \code{navbarPage()} / \code{tabsetPanel()}.}

\item{tabs}{Optional character vector of valid page names. Hash routes
naming other pages trigger a one-time warning and fall back to \code{default}.
\code{NULL} accepts any page.}

\item{default}{Fallback page for unknown hash routes. \code{NULL} warns and
stays on the current tab.}

\item{hash}{Optional reactive returning the current URL hash. Defaults to
\code{session$clientData$url_hash}; override it in tests, where the mock
session's hash is not settable.}
}
\value{
Invisibly, a list with reactives \code{page} (current page name, or
\code{NULL} before first navigation) and \code{parts} (all decoded \code{/}-separated
segments of the route, e.g. \code{c("search", "extra")} for \verb{#!/search/extra};
segments beyond the page name are preserved but do not affect
navigation).
}
\description{
Keeps the selected tab of a \code{\link[shiny:navbarPage]{shiny::navbarPage()}} / \code{\link[shiny:tabsetPanel]{shiny::tabsetPanel()}}
in sync with a hash route of the form \verb{#!/page}. Opening the app at
\verb{https://myapp/#!/search} navigates to the \code{search} tab, switching tabs
pushes \verb{#!/<tab>} onto the browser history, and the browser back/forward
buttons navigate between tabs. Hashes that do not start with \verb{#!/} (plain
in-page anchors) are ignored.
}
\details{
Usually you do not call this directly: pass \code{routing = "hash"} to
\code{\link[=serve_dormant]{serve_dormant()}}, which wires the router to its components. Call it
directly to add URL routing to a multi-page app that does not use the
dormant lifecycle.
}
\examples{
\dontrun{
server <- function(input, output, session) {
  route <- router_server(session, input, navbar = "pages",
                         tabs = c("dashboard", "search"),
                         default = "dashboard")
  # route$page() is the current page as a reactive
}
}
}
