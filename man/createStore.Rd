% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/store.R
\name{createStore}
\alias{createStore}
\title{Create a shared store}
\usage{
createStore(...)
}
\arguments{
\item{...}{Named initial values.}
}
\value{
A \code{shinystate_store} object with methods:
\describe{
\item{\verb{$get(name = NULL)}}{Read one field, or a snapshot of all fields.}
\item{\verb{$set(...)}}{Update named fields and notify subscribers.}
\item{\verb{$subscribe(fn)}}{Register a listener; returns an unsubscribe
function.}
}
}
\description{
A store holds state that several components can read and write, like a Pinia
or Zustand store. Components subscribe with \code{\link[=useStore]{useStore()}} and re-render when
the store changes. Use the returned object's \verb{$get()} / \verb{$set()} methods to
read and write from anywhere (e.g. inside a click handler).
}
\examples{
store <- createStore(count = 0L)
store$set(count = 1L)
store$get("count")
}
\seealso{
\code{\link[=useStore]{useStore()}}
}
